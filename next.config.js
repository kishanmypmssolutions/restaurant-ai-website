/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you need bodyParser:false for specific API routes, remove this and set config per API file.
  // Next.js no longer accepts `api` top-level config in next.config.js
}

module.exports = nextConfig
